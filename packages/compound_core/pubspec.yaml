name: compound_core
description: Shared models, repositories, and services for the compound app.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cloud_firestore: ^6.2.0
  firebase_auth: ^6.3.0
  firebase_storage: ^13.2.0
  firebase_messaging: ^16.1.0
  flutter_local_notifications: ^17.2.2
  excel: ^4.0.6
  equatable: ^2.0.5
  intl: ^0.20.2
  flutter:
    sdk: flutter
